# Architecture Decision Records (ADR)

lotto-data 파이프라인(크롤링·정제·배포)의 주요 설계 결정을 기록한다. 각 ADR은 상황(Context) / 선택(Decision) / 포기한 옵션(Considered Alternatives) / 근거(Rationale) / 결과(Consequences) 구조를 따른다.

이 저장소는 데이터 **생산자**다. 데이터를 **소비**하는 앱(LottoStats) 관점의 결정은 해당 저장소의 `docs/DECISIONS.md`에 있다(특히 앱 ADR-06이 본 저장소 ADR-01과 짝을 이룬다).

---

## ADR-01: 데이터를 별도 공개 저장소에 정적 JSON으로 호스팅

- **상황**: 매주 갱신되는 로또 당첨번호를 앱·외부에 공급해야 함. 백엔드 서버 운영은 비용·인증·유지보수 부담.
- **선택**: 본 저장소(`JunKwon91/lotto-data`)를 **데이터 단일 소스**로 삼아 `data/lotto-history.json`을 **GitHub raw URL**로 공개 제공한다. 앱·외부는 이 URL을 직접 fetch.
- **포기한 옵션**: 앱 저장소에 데이터 동봉(회차마다 앱 재배포 필요), 자체 API 서버, npm 패키지 배포.
- **근거**: GitHub raw URL이 CDN·무인증·무료 호스팅을 대신함 → 이 URL 한 줄이 사실상 "API". 데이터가 앱과 분리되어 새 회차가 추가돼도 앱 재배포가 불필요하고, 누구나 동일 데이터를 호출 가능.
- **결과**: 공급 계약은 `data/lotto-history.json` + README 명시 스키마. 소비자(앱)의 `LOTTO_DATA_URL`이 이 파일을 가리킴.

---

## ADR-02: 초기 데이터 부트스트랩을 smok95/lotto에서 가져옴

- **상황**: 자동 갱신을 시작하려면 1회차(2002-12-07)부터 현재까지 누적분이 먼저 있어야 함. 1,200회+를 직접 크롤링하면 시간·서버 부담 큼.
- **선택**: 공개 데이터셋 **smok95/lotto**에서 1~1223회차를 일괄 확보해 부트스트랩하고, 이후부터 자체 크롤링으로 증분 갱신.
- **포기한 옵션**: 1회차부터 전량 자체 크롤링(과도한 요청·시간), 수기 입력.
- **근거**: 검증된 오픈 데이터셋으로 초기 적재를 끝내면 정확성·속도 모두 유리. 이후는 주당 1회차만 추가하면 됨.
- **결과**: 원본은 `smok95-source/`에 보존하되 변환본만 커밋. 변환은 일회성 스크립트로 수행 후 제거(`29f5aea`). → 스키마 간소화는 ADR-03, 원본 추적 제외는 ADR-04.

---

## ADR-03: 공유 JSON 스키마를 당첨번호 4필드로 간소화

- **상황**: smok95 원본은 상금 구간(`divisions`)·총판매액·배출점·당첨조합 등 풍부한 필드를 포함. 소비 앱은 통계 분석용이라 당첨번호만 필요.
- **선택**: 회차당 **`drawNo` / `date` / `numbers`(6개, 오름차순) / `bonusNo`** 4필드로 한정. 전체 응답은 `{ updatedAt, latestRound, data[] }`.
- **포기한 옵션**: 원본 전체 필드 보존(상금·판매·배출점 포함).
- **근거**: 소비처가 쓰지 않는 필드는 페이로드만 키우고(과거 회차 불변 데이터를 매번 다운로드) 스키마 결합도를 높임. 당첨번호만 유지하면 파일 크기·계약이 단순.
- **결과**: 앱 `src/types/lotto.ts`와 1:1 일치하는 계약. **스키마 변경 시 앱과 동시 수정 필요.** 상금·배출점이 후일 필요해지면 별도 엔드포인트 파일로 분리(현 계약 불변 유지).

---

## ADR-04: 원본 데이터(`smok95-source/`)를 git 추적 제외

- **상황**: 부트스트랩 원본(smok95 클론)을 저장소에 포함할지 여부.
- **선택**: `.gitignore`에 `smok95-source/` 등록 — **로컬 보존, 커밋 제외**.
- **포기한 옵션**: 원본까지 커밋(저장소 비대화·라이선스 혼선), 원본 완전 삭제(재현 불가).
- **근거**: 원본은 외부 저장소에서 언제든 재클론 가능하므로 추적할 필요가 없고, 우리 산출물은 변환된 `data/lotto-history.json`뿐. 저장소를 산출물 중심으로 가볍게 유지.
- **결과**: 원본이 로컬에만 존재 → 변환을 재현하려면 smok95/lotto를 다시 클론해야 함(트레이드오프 인지).

---

## ADR-05: 주간 자동 갱신을 GitHub Actions cron으로 수행

- **상황**: 매주 토요일 추첨 후 새 회차를 무인으로 추가해야 함.
- **선택**: **GitHub Actions cron** — `0 13 * * 6`(토 13:00 UTC = 22:00 KST, 추첨 후 약 1.5h 여유) + `workflow_dispatch` 수동 트리거. 데이터 변경 시 봇이 자동 commit·push.
- **포기한 옵션**: AWS Lambda + EventBridge, 자체 서버 cron, Cloudflare Workers Cron.
- **근거**: 데이터가 사는 저장소 **안에서 self-contained**하게 갱신·커밋 가능. public repo는 무료, 권한은 `contents: write` 한 줄. 별도 인프라·계정 불필요.
- **결과**: `.github/workflows/weekly-update.yml`. 런타임은 Node 22 LTS + Playwright Chromium 설치 스텝 포함. 실패 처리는 ADR-08.

---

## ADR-06: 증분 갱신 — 이론상 최신 회차 계산 + 갭만큼만 순차 크롤링

- **상황**: 매 실행 시 어디까지 크롤링해야 하는지 알아야 하고, 사이트 과부하·폭주를 막아야 함.
- **선택**: 1회차(2002-12-07 토 20:35 KST) 기준 주 단위로 **이론상 현재 회차**를 계산 → 저장된 `latestRound`와의 **갭만큼만** `lastRound+1 … current` 순차 크롤링. 회차 간 **1초 sleep**, **`MAX_GAP` 10주 초과 시 중단**.
- **포기한 옵션**: 매번 1회차부터 전량 재크롤링, 고정 개수 크롤링, 간격 없는 연속 요청.
- **근거**: 증분만 받으므로 요청 최소화. 1초 간격은 서버 부담 완화. 갭 가드는 데이터 이상(음수 갭)·장기 다운(10주+) 같은 비정상을 자동 중단해 오염을 예방(이상 시 수동 개입 유도). 중복 회차는 방어적으로 스킵.
- **결과**: `scripts/update.js`. 정상 주에는 1개 회차만 추가. 비정상은 throw → 워크플로우 실패 → 이슈 생성(ADR-08).

---

## ADR-07: 크롤러를 axios+cheerio → Playwright(헤드리스 Chromium)로 전환

- **상황**: 초기 크롤러는 `axios`로 옛 페이지(`gameResult.do?method=byWin`) HTML을 받아 `cheerio`로 파싱. 2026년 1월 동행복권 사이트가 SPA로 리뉴얼되며 옛 페이지가 404가 되고, 새 JSON 엔드포인트는 axios/curl 같은 **비-브라우저 트래픽을 차단**(연결 타임아웃).
- **선택**: **Playwright 헤드리스 Chromium**으로 실제 결과 페이지(`/lt645/result`)를 띄운 뒤, **그 페이지 컨텍스트 안에서 `fetch`**로 동일 origin API(`selectPstLt645InfoNew.do?srchLtEpsd=…`)를 호출해 JSON을 받는다.
- **포기한 옵션**: axios+cheerio 유지(차단으로 동작 불가), 비공식 미러 API(안정성 의존), User-Agent만 위조한 직접 요청(엔드포인트가 origin·세션 검사로 차단).
- **근거**: 새 엔드포인트는 동일 origin·브라우저 세션을 요구 → 실제 페이지 안의 fetch만 통과. 브라우저는 싱글톤으로 재사용해 오버헤드 최소화. 응답 필드(`tm1~6WnNo`/`bnsWnNo`/`ltRflYmd`)를 4필드 스키마로 정규화 + 범위(1–45)·형식 3중 검증.
- **결과**: `scripts/crawl-dhlottery.js`가 `playwright` 의존(`cheerio`는 사실상 미사용). Actions가 매 실행마다 `playwright install --with-deps chromium` 수행(실행 시간 증가는 인지된 비용). **사이트 구조 재변경 시 이 ADR의 셀렉터·엔드포인트가 1차 점검 지점.**

---

## ADR-08: 갱신 실패 시 GitHub 이슈 자동 생성

- **상황**: 무인 운영이라 크롤러가 깨져도(사이트 구조 변경·다운·갭 초과) 즉시 알기 어려움.
- **선택**: 워크플로우 `failure()` 시 **이슈 자동 생성**(`actions/github-script`) — 실패 일시·로그 URL·가능 원인·대응 가이드 포함.
- **포기한 옵션**: 알림 없음(조용한 실패), 외부 알림 채널(Slack 등, 별도 연동·시크릿 필요).
- **근거**: 이슈는 저장소 내장 채널이라 추가 연동·시크릿이 없고, 실패 맥락(원인 후보·재실행 방법)을 한곳에 남겨 추적·대응이 빠름. `issues: write` 권한 한 줄.
- **결과**: 셀렉터/엔드포인트 깨짐(ADR-07)·장기 다운(ADR-06)이 이슈로 가시화됨. 대응은 수동 점검 후 `workflow_dispatch` 재실행.

---

## ADR-09: 문서화 방식 — ADR 채택

- **상황**: 그간 결정 근거가 커밋 메시지·README에 흩어져 있어, 사이트 리뉴얼 대응(ADR-07)·스키마 간소화(ADR-03) 같은 "왜"가 추적되지 않음.
- **선택**: 형제 프로젝트(LottoStats 앱, rn-design-system 라이브러리)와 **동일한 ADR 방식**을 본 `DECISIONS.md`로 채택.
- **포기한 옵션**: README에 결정 누적(레퍼런스와 의사결정 혼재), 커밋 메시지에만 의존(맥락 단편화).
- **근거**: ADR은 "왜 이 선택을 했나"를 코드 변경과 무관하게 보존 → 무인 파이프라인이 깨졌을 때 1차 점검 지점과 설계 원칙을 즉시 참조 가능. 세 저장소가 같은 형식이라 모노레포 전반의 문서 일관성 확보.
- **결과**: README는 사용법·스키마 **레퍼런스**, `DECISIONS.md`는 **의사결정 기록**으로 역할 분리. 신규 결정은 모두 이 문서에 ADR로 추가한다.
