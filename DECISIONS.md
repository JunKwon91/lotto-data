# Architecture Decision Records (ADR)

lotto-data 파이프라인(크롤링·정제·배포)의 주요 설계 결정을 기록한다. 각 ADR은 상황(Context) / 선택(Decision) / 포기한 옵션(Considered Alternatives) / 근거(Rationale) / 결과(Consequences) 구조를 따른다.

이 저장소는 데이터 **생산자**다. 데이터를 **소비**하는 앱(LottoStats) 관점의 결정은 해당 저장소의 `docs/DECISIONS.md`에 있다(특히 앱 ADR-06이 본 저장소 ADR-01과 짝을 이룬다).

---

## ADR-01: 데이터를 별도 공개 저장소에 정적 JSON으로 호스팅

- **상황**: 매주 갱신되는 로또 당첨번호를 앱·외부에 공급해야 함. 백엔드 서버 운영은 비용·인증·유지보수 부담.
- **선택**: 본 저장소(`JunKwon91/lotto-data`)를 **데이터 단일 소스**로 삼아 `data/lotto-history.json`을 **GitHub raw URL**로 공개 제공한다. 앱·외부는 이 URL을 직접 fetch.
- **포기한 옵션**: 앱 저장소에 데이터 동봉(회차마다 앱 재배포 필요), 자체 API 서버, npm 패키지 배포.
- **근거**: GitHub raw URL이 CDN·무인증·무료 호스팅을 대신함 → 이 URL 한 줄이 사실상 "API". 데이터가 앱과 분리되어 새 회차가 추가돼도 앱 재배포가 불필요하고, 누구나 동일 데이터를 호출 가능.
- **결과**: 공급 계약은 `data/lotto-history.json` + README 명시 스키마. 소비자(앱)의 `LOTTO_DATA_URL`이 이 파일을 가리킴.

---

## ADR-02: 초기 데이터 부트스트랩을 smok95/lotto에서 가져옴

- **상황**: 자동 갱신을 시작하려면 1회차(2002-12-07)부터 현재까지 누적분이 먼저 있어야 함. 1,200회+를 직접 크롤링하면 시간·서버 부담 큼.
- **선택**: 공개 데이터셋 **smok95/lotto**에서 1~1223회차를 일괄 확보해 부트스트랩하고, 이후부터 자체 크롤링으로 증분 갱신.
- **포기한 옵션**: 1회차부터 전량 자체 크롤링(과도한 요청·시간), 수기 입력.
- **근거**: 검증된 오픈 데이터셋으로 초기 적재를 끝내면 정확성·속도 모두 유리. 이후는 주당 1회차만 추가하면 됨.
- **결과**: 원본은 `smok95-source/`에 보존하되 변환본만 커밋. 변환은 일회성 스크립트로 수행 후 제거(`29f5aea`). → 스키마 간소화는 ADR-03, 원본 추적 제외는 ADR-04.

---

## ADR-03: 공유 JSON 스키마를 당첨번호 4필드로 간소화

- **상황**: smok95 원본은 상금 구간(`divisions`)·총판매액·배출점·당첨조합 등 풍부한 필드를 포함. 소비 앱은 통계 분석용이라 당첨번호만 필요.
- **선택**: 회차당 **`drawNo` / `date` / `numbers`(6개, 오름차순) / `bonusNo`** 4필드로 한정. 전체 응답은 `{ updatedAt, latestRound, data[] }`.
- **포기한 옵션**: 원본 전체 필드 보존(상금·판매·배출점 포함).
- **근거**: 소비처가 쓰지 않는 필드는 페이로드만 키우고(과거 회차 불변 데이터를 매번 다운로드) 스키마 결합도를 높임. 당첨번호만 유지하면 파일 크기·계약이 단순.
- **결과**: 앱 `src/types/lotto.ts`와 1:1 일치하는 계약. **스키마 변경 시 앱과 동시 수정 필요.** 상금·배출점이 후일 필요해지면 별도 엔드포인트 파일로 분리(현 계약 불변 유지).

> **개정 (2026-07, ADR-11)**: 앱 홈·상세 화면이 상금 정보를 요구하여 "4필드 한정" 결정을 뒤집고 회차 상세 필드(등수별 상금·인원, 총계, 1등 유형)를 같은 파일에 추가했다. 원 결정(4필드)의 근거는 위 기록으로 보존하고, 확장된 계약은 ADR-11을 따른다. "별도 엔드포인트 파일로 분리"는 채택하지 않았다(단일 파일 유지, 아래 ADR-11 근거).

---

## ADR-04: 원본 데이터(`smok95-source/`)를 git 추적 제외

- **상황**: 부트스트랩 원본(smok95 클론)을 저장소에 포함할지 여부.
- **선택**: `.gitignore`에 `smok95-source/` 등록 — **로컬 보존, 커밋 제외**.
- **포기한 옵션**: 원본까지 커밋(저장소 비대화·라이선스 혼선), 원본 완전 삭제(재현 불가).
- **근거**: 원본은 외부 저장소에서 언제든 재클론 가능하므로 추적할 필요가 없고, 우리 산출물은 변환된 `data/lotto-history.json`뿐. 저장소를 산출물 중심으로 가볍게 유지.
- **결과**: 원본이 로컬에만 존재 → 변환을 재현하려면 smok95/lotto를 다시 클론해야 함(트레이드오프 인지).

---

## ADR-05: 주간 자동 갱신을 GitHub Actions cron으로 수행

- **상황**: 매주 토요일 추첨 후 새 회차를 무인으로 추가해야 함.
- **선택**: **GitHub Actions cron** — `0 13 * * 6`(토 13:00 UTC = 22:00 KST, 추첨 후 약 1.5h 여유) + `workflow_dispatch` 수동 트리거. 데이터 변경 시 봇이 자동 commit·push.
- **포기한 옵션**: AWS Lambda + EventBridge, 자체 서버 cron, Cloudflare Workers Cron.
- **근거**: 데이터가 사는 저장소 **안에서 self-contained**하게 갱신·커밋 가능. public repo는 무료, 권한은 `contents: write` 한 줄. 별도 인프라·계정 불필요.
- **결과**: `.github/workflows/weekly-update.yml`. 런타임은 Node 22 LTS + Playwright Chromium 설치 스텝 포함. 실패 처리는 ADR-08.

> **개정 (2026-07, ADR-15)**: 러너의 브라우저 설치를 번들 Chromium → 시스템 Chrome(`playwright install --with-deps chrome`)으로 교체했다(봇 방어 통과). cron·권한·실패 처리 구성은 무변경.

---

## ADR-06: 증분 갱신 — 이론상 최신 회차 계산 + 갭만큼만 순차 크롤링

- **상황**: 매 실행 시 어디까지 크롤링해야 하는지 알아야 하고, 사이트 과부하·폭주를 막아야 함.
- **선택**: 1회차(2002-12-07 토 20:35 KST) 기준 주 단위로 **이론상 현재 회차**를 계산 → 저장된 `latestRound`와의 **갭만큼만** `lastRound+1 … current` 순차 크롤링. 회차 간 **1초 sleep**, **`MAX_GAP` 10주 초과 시 중단**.
- **포기한 옵션**: 매번 1회차부터 전량 재크롤링, 고정 개수 크롤링, 간격 없는 연속 요청.
- **근거**: 증분만 받으므로 요청 최소화. 1초 간격은 서버 부담 완화. 갭 가드는 데이터 이상(음수 갭)·장기 다운(10주+) 같은 비정상을 자동 중단해 오염을 예방(이상 시 수동 개입 유도). 중복 회차는 방어적으로 스킵.
- **결과**: `scripts/update.js`. 정상 주에는 1개 회차만 추가. 비정상은 throw → 워크플로우 실패 → 이슈 생성(ADR-08).

---

## ADR-07: 크롤러를 axios+cheerio → Playwright(헤드리스 Chromium)로 전환

- **상황**: 초기 크롤러는 `axios`로 옛 페이지(`gameResult.do?method=byWin`) HTML을 받아 `cheerio`로 파싱. 2026년 1월 동행복권 사이트가 SPA로 리뉴얼되며 옛 페이지가 404가 되고, 새 JSON 엔드포인트는 axios/curl 같은 **비-브라우저 트래픽을 차단**(연결 타임아웃).
- **선택**: **Playwright 헤드리스 Chromium**으로 실제 결과 페이지(`/lt645/result`)를 띄운 뒤, **그 페이지 컨텍스트 안에서 `fetch`**로 동일 origin API(`selectPstLt645InfoNew.do?srchLtEpsd=…`)를 호출해 JSON을 받는다.
- **포기한 옵션**: axios+cheerio 유지(차단으로 동작 불가), 비공식 미러 API(안정성 의존), User-Agent만 위조한 직접 요청(엔드포인트가 origin·세션 검사로 차단).
- **근거**: 새 엔드포인트는 동일 origin·브라우저 세션을 요구 → 실제 페이지 안의 fetch만 통과. 브라우저는 싱글톤으로 재사용해 오버헤드 최소화. 응답 필드(`tm1~6WnNo`/`bnsWnNo`/`ltRflYmd`)를 4필드 스키마로 정규화 + 범위(1–45)·형식 3중 검증.
- **결과**: `scripts/crawl-dhlottery.js`가 `playwright` 의존(`cheerio`는 사실상 미사용). Actions가 매 실행마다 `playwright install --with-deps chromium` 수행(실행 시간 증가는 인지된 비용). **사이트 구조 재변경 시 이 ADR의 셀렉터·엔드포인트가 1차 점검 지점.**

> **개정 (2026-07, ADR-15)**: 봇 방어가 번들 Chromium 연결을 차단하게 되어 실행 브라우저를 시스템 Chrome 채널(`channel: 'chrome'`)로 전환했다. 접근 방식(결과 페이지 진입 후 페이지 내 `fetch`)·엔드포인트·필드 정규화는 그대로이고, 실행 바이너리만 교체했다.

---

## ADR-08: 갱신 실패 시 GitHub 이슈 자동 생성

- **상황**: 무인 운영이라 크롤러가 깨져도(사이트 구조 변경·다운·갭 초과) 즉시 알기 어려움.
- **선택**: 워크플로우 `failure()` 시 **이슈 자동 생성**(`actions/github-script`) — 실패 일시·로그 URL·가능 원인·대응 가이드 포함.
- **포기한 옵션**: 알림 없음(조용한 실패), 외부 알림 채널(Slack 등, 별도 연동·시크릿 필요).
- **근거**: 이슈는 저장소 내장 채널이라 추가 연동·시크릿이 없고, 실패 맥락(원인 후보·재실행 방법)을 한곳에 남겨 추적·대응이 빠름. `issues: write` 권한 한 줄.
- **결과**: 셀렉터/엔드포인트 깨짐(ADR-07)·장기 다운(ADR-06)이 이슈로 가시화됨. 대응은 수동 점검 후 `workflow_dispatch` 재실행.

---

## ADR-09: 문서화 방식 — ADR 채택

- **상황**: 그간 결정 근거가 커밋 메시지·README에 흩어져 있어, 사이트 리뉴얼 대응(ADR-07)·스키마 간소화(ADR-03) 같은 "왜"가 추적되지 않음.
- **선택**: 형제 프로젝트(LottoStats 앱, rn-design-system 라이브러리)와 **동일한 ADR 방식**을 본 `DECISIONS.md`로 채택.
- **포기한 옵션**: README에 결정 누적(레퍼런스와 의사결정 혼재), 커밋 메시지에만 의존(맥락 단편화).
- **근거**: ADR은 "왜 이 선택을 했나"를 코드 변경과 무관하게 보존 → 무인 파이프라인이 깨졌을 때 1차 점검 지점과 설계 원칙을 즉시 참조 가능. 세 저장소가 같은 형식이라 모노레포 전반의 문서 일관성 확보.
- **결과**: README는 사용법·스키마 **레퍼런스**, `DECISIONS.md`는 **의사결정 기록**으로 역할 분리. 신규 결정은 모두 이 문서에 ADR로 추가한다.

---

## ADR-10: 수집 스크립트는 명시적으로 브라우저를 닫고 exit 코드로 종료한다

- **상황**: `update.js`가 다회차 수집을 마치고 "✅ 갱신 완료"를 출력한 뒤에도 종료되지 않아, GitHub Actions가 6시간 타임아웃까지 매달리고(또는 1시간+ 후 실패) `Commit and push` 스텝이 누락돼 데이터가 1227에 정체됨. 단발 수집(맥 직접·Actions 수동 진단)은 통과했고 정기 cron의 다회차 경로에서만 드러남.
- **선택**: 수집 루프·파일 쓰기 직후 `closeBrowser()`를 **명시적으로 await**해 Chromium을 닫고, `main()` 완료 시 `process.exit(0)` / 실패 시 `process.exit(1)`로 종료 코드를 명확히 한다.
- **포기한 옵션**: `process.on('beforeExit')` 자동 정리에만 의존(ADR-07의 초기 방식).
- **근거**: `beforeExit`는 **이벤트 루프가 빌 때만** 발화하는데, 열린 Chromium이 활성 핸들(서브프로세스·파이프)로 루프를 계속 붙잡아 영영 발화하지 않는다. 진단 경로는 워크플로의 `finally`에서 명시적으로 닫아 통과했으나 cron 경로엔 명시적 닫기가 없어 실전에서 처음 노출됐다. 명시적 종료는 잔여 핸들 유무와 무관하게 프로세스 종료를 보장하고, exit 0/1 구분으로 Actions의 commit·push / 실패 이슈 분기가 올바로 동작한다.
- **결과**: cron과 동일 경로가 실전에서 1분 내 정상 완료·commit·push 확인(2026-06-30, latestRound 1230 갱신). `closeBrowser()`는 기존 idempotent 가드(호출 후 null화)로 중복 호출에 안전. 접근·API·파싱(ADR-07)은 무변경.

---

## ADR-11: 공유 JSON 스키마에 회차 상세 필드 추가 (ADR-03 확장)

- **상황**: 앱 화면이 당첨번호만으로 부족해짐. 홈 Hero는 1등 총 당첨금, RoundDetail은 등수별 1인당 당첨금·당첨인원 표와 총 당첨자 수·총 당첨금액을 요구한다. ADR-03은 페이로드·결합도를 이유로 4필드만 저장하기로 했었다.
- **선택**: `data[]` 각 회차에 **옵셔널 확장 필드**를 추가한다 — `prizes`(1~5등 각 `{ rank, winners, prizePerWinner }`), `totalWinners`(총 당첨자 수), `totalPrize`(총 당첨금액), `totalSales`(총 판매액), `firstWinMethod`(1등 유형별 게임수 `{ auto, manual, semiAuto }`). 원천은 기존과 같은 `selectPstLt645InfoNew.do` 응답의 그동안 버리던 필드다. 별도 엔드포인트 파일 분리(ADR-03이 제안했던 방식)는 **채택하지 않고 단일 파일에 통합**했다.
- **포기한 옵션**: 4필드 유지(화면 요구 미충족), 상금만 별도 `prizes/{회차}.json`로 분리(ADR-03 제안 — 소비 측 fetch·캐시 복잡도 증가, 회차 상세는 항상 회차와 함께 필요).
- **근거**: 확장 후에도 파일은 약 1MB 수준으로 앱이 1회 로드·캐시하는 구조에 무리가 없어(측정 근거), 분리로 얻을 이득보다 단일 계약의 단순함이 크다. 신규 필드를 **모두 옵셔널**로 둬, 백필 전 구 회차·구 캐시가 있어도 소비 측이 안전하게 undefined를 다룬다(생산·소비 동시 변경 부담 완화). 당첨번호·보너스·날짜 4필드는 형태·값 모두 불변으로 유지.
- **결과**: 크롤러 파싱은 `parseExtended`(commit `c7d6ffd`)에서 단일 지점으로 확장, 전 회차 백필로 데이터 반영(commit `740e8e4`, 1~1230). 앱 `src/types/lotto.ts`도 옵셔널로 동기화(앱 레포). 개별 필드의 원천 품질 대응은 ADR-12·13·14, 백필 실행 방식은 ADR-16 참조. **소비 측(앱) 캐시 무효화는 의도적으로 보류**한다 — 앱 미출시·설치 사용자가 없어 무효화할 구 캐시가 존재하지 않기 때문. 앱 출시 또는 다음 스키마 변경 시점에 무효화 로직과 함께 도입하며, 그 결정은 앱 레포 `docs/DECISIONS.md`에 기록한다(빠뜨린 게 아니라 미룬 것).

---

## ADR-12: totalPrize는 원천 배당 필드 대신 등수별 값의 합으로 계산한다

- **상황**: 총 당첨금액 후보로 원천에 `rlvtEpsdSumNtslAmt`가 있으나, 전수 점검 결과 **1~260회차에서 이 값이 총 판매액(`wholEpsdSumNtslAmt`)과 동일한 오류값**(실제 배당의 약 2배)이었다. 예: 3회차 원천 4,729,342,000 = 판매액이지만, 실제 등수별 배당 합은 3,221,865,900.
- **선택**: `totalPrize`를 원천 `rlvtEpsdSumNtslAmt`가 아니라 **`Σ(prizes[].winners × prizePerWinner)`로 계산**해 저장한다(`crawl-dhlottery.js:121`).
- **포기한 옵션**: 원천값 그대로 저장(초기 회차 오류 잔존), 초기 회차만 원천값 제거(구간 분기·null 발생), 초기 회차만 계산·이후 원천값(정의 이원화).
- **근거**: 등수별 `winners`·`prizePerWinner`는 smok95 교차대조로 검증된 신뢰값이라 그 합이 실제 지급 총액이다. 261회차 이후는 계산값과 원천값이 반올림 수준(수백 원)만 차이 나 B단위 화면 표시에 영향이 없고, 단일 정의(구간 분기·null 없음)로 전 회차 일관을 얻는다.
- **결과**: 반영 후 전 회차 `totalPrize === Σ(winners × prizePerWinner)` 성립, `totalPrize === totalSales` 0건(초기 오류 해소). 향후 크롤도 계산 정의를 따르므로 신·구 회차가 일관(commit `b41a3df` + 데이터 `740e8e4`).

---

## ADR-13: firstWinMethod — winType 매핑 확정 및 초기 미제공 회차 키 생략

- **상황**: 1등 게임유형(자동/수동/반자동)이 원천 `winType0~3`에 담기나 인덱스별 의미가 불명확했고, 초기 회차는 이 값이 전부 0으로 온다.
- **선택**: 매핑을 **`winType1`=자동, `winType2`=수동, `winType3`=반자동, `winType0`=미사용(항상 0, 저장 안 함)**으로 확정한다. 원천이 유형을 제공하는 **262회차부터** `firstWinMethod`를 저장하고, 그 이전 미제공 회차(1등 당첨자는 있으나 winType 합이 0)는 **키 자체를 생략**한다. 1등 당첨자가 0명인 이월 회차는 `{0,0,0}`이 실제와 정합하므로 그대로 둔다.
- **포기한 옵션**: 인덱스 의미를 추측으로 확정(검증 없이 위험), 미제공 회차를 `{0,0,0}`으로 저장(1등 당첨자가 있는데 "0개씩"으로 오해됨).
- **근거**: 매핑은 **10개 회차 실측 + smok95 6개 회차(`winners_combination` auto/manual/semi_auto)와의 값 일치**로 확정했다(추측 아님). `winType1+2+3 = 1등 인원`이 성립하고 `winType0`은 전 회차 0이었다. 미제공을 키 생략(`undefined`)으로 두면 "유형 정보 없음"이 명확해 오독을 막는다.
- **결과**: 저장은 262회차부터 `firstWinMethod` 존재, 이전은 키 없음(백필 후처리로 250건 생략). 초기 회차의 유형 데이터 부재는 원천 특성이며 값 오류가 아니다. 반영 데이터는 commit `740e8e4`.

---

## ADR-14: 저장하지 않는 파생필드의 정합 검증은 throw가 아닌 warn

- **상황**: 파싱 시 `winners × prizePerWinner === rnkNSumWnAmt`(원천 등수별 총액)를 예외로 강제했더니, **1등 0명 이월 회차(0×0=0 ≠ 원천 이월 풀 값)와 초기 회차(원천 SumWnAmt=0)** 등 유효 회차가 거부됐다. 백필에서는 이 거부가 회차마다 컨텍스트 재생성을 유발해 연결 폭주로 이어졌다.
- **선택**: `rnkNSumWnAmt`처럼 **스키마에 저장하지 않는 파생필드**의 교차검증은 **경고(로그)로만** 남긴다. 저장하는 값의 검증(유한수·음수 불가, `Σwinners === totalWinners`)만 유지한다. 초기 이월 회차의 1등은 `winners=0` 실값으로 정상 저장한다.
- **포기한 옵션**: throw 유지(유효 회차 거부·대량 수집 중단), 검증 전면 제거(진짜 이상값도 못 걸러냄).
- **근거**: 저장하지 않는 파생필드가 저장값을 무효화할 이유가 없다. 원천이 초기 회차에 이 필드를 0·이월값으로 불완전하게 주는 것은 확인된 사실이므로, 그걸로 유효한 저장값을 버리면 안 된다. 관측은 warn으로 유지해 이상 패턴 추적은 가능하게 한다.
- **결과**: 1등 0명 회차 포함 전 회차가 예외 없이 통과. `crawl-dhlottery.js`의 SumWnAmt 검증이 throw→warn으로 하향(commit `b41a3df`). 대량 백필의 오류 종류 구분은 ADR-16.

---

## ADR-15: 실행 브라우저를 번들 Chromium → 시스템 Chrome 채널로 전환

- **상황**: 백필 대량 수집 도중부터 동행복권 접근이 연결 단계에서 무응답(page.goto 타임아웃, `about:blank`)이 됐다. 초기에는 반복 크롤로 인한 **IP 스로틀로 오진**했다.
- **선택**: `chromium.launch({ channel: process.env.PLAYWRIGHT_CHANNEL || 'chrome' })`로 **시스템 설치 Chrome**을 사용한다(`crawl-dhlottery.js:49`). CI도 `playwright install --with-deps chrome`으로 Chrome을 설치한다(`weekly-update.yml`).
- **포기한 옵션**: IP 쿨다운 대기(오진 — 원인 아님), 헤디드 실행(번들 Chromium은 헤디드로도 차단), User-Agent 변경(연결 단계 차단이라 무효).
- **근거**: 격리 테스트로 원인을 규명했다 — ①같은 IP에서 실제 브라우저는 정상 접속, ②Playwright가 example.com 등 타 사이트는 정상, ③번들 Chromium은 헤드리스·헤디드 모두 dhlottery만 차단, ④`channel: 'chrome'`(실제 Chrome 지문)은 통과. 즉 IP가 아니라 **봇 방어의 브라우저/지문 차단**이 원인이었다. **교훈: 네트워크 타임아웃 증상을 IP 문제로 단정하지 말고, 브라우저·지문 변수부터 격리해 확인할 것.**
- **결과**: 로컬·CI 모두 Chrome 채널로 봇 방어 통과. `workflow_dispatch` 수동 검증(2026-07-02)에서 Chrome 설치·1227회차 진단 크롤 성공·update 정상 종료(53초)·commit/push 스텝 정상("변경 없음", 새 회차 미출현) 확인. **다만 새 회차(1231, 2026-07-04 예정)를 실제로 커밋하는 정기 실행은 아직 관측되지 않았다**(수동 트리거까지만 검증). commit `b41a3df`(크롤러) + `a15176a`(CI). ADR-05·07의 브라우저·설치 항목을 개정.

---

## ADR-16: 대량 백필 — 저장 경로 공유, 오류 종류 구분, 체크포인트·resume

- **상황**: 확장 스키마를 1~1230 전 회차에 채우려면 대량 재수집이 필요한데, 봇 방어·큐로 인한 중단이 확정적이라 중단 내성과 이어받기가 있어야 했다. 또 백필이 저장 규칙을 자체 구현하면 주간 갱신과 드리프트할 위험이 있었다.
- **선택**: (1) `update.js`에 인라인이던 병합·저장 로직을 `scripts/lotto-store.js`로 추출해 주간 갱신과 백필이 **동일 저장 경로**를 재사용한다. (2) `scripts/backfill.js`는 오류를 **네트워크(`err.kind==='network'`) vs 파싱/검증**으로 구분해, 네트워크만 재시도+컨텍스트 재생성하고 파싱/검증 오류는 즉시 실패 기록 후 다음 회차로 진행한다. (3) N회마다 출력·체크포인트 증분 저장 + 재실행 시 이미 받은 회차 건너뛰기(resume) + 실패 회차 리포트 + 종료 패턴(ADR-10)을 적용한다. 병합 정책은 **덮어쓰기**(재크롤로 새 스키마 교체)다. 회차 간 간격은 환경변수로 상향 가능(스로틀 예방).
- **포기한 옵션**: 백필이 저장 로직을 자체 구현(드리프트 위험), 모든 오류에서 컨텍스트 재생성(파싱 오류에도 재생성해 연결 폭주 — 실제 발생), 무중단 일괄 실행(중단 시 처음부터).
- **근거**: 저장 경로 공유로 dedup·정렬·메타 형식이 한 곳에만 존재해 데이터 형태가 회차마다 흔들리지 않는다. 오류 구분은 재생성이 효과 있는 경우(네트워크)에만 적용해, ADR-14가 없앤 검증-throw와 맞물려 재생성 폭주를 원천 차단한다. 체크포인트·resume은 봇 방어 중단을 정상 흐름으로 흡수한다.
- **결과**: 간격 상향 + Chrome 채널(ADR-15)로 1~1230 전량 수집 성공(재시도는 있었으나 최종 실패 0). Gate 검증(완결성·정합·smok95 교차대조) 통과 후 데이터 반영(commit `740e8e4`). `lotto-store.js`는 커밋(`c7d6ffd`), `backfill.js`·프로브는 **일회성 도구라 `.gitignore`로 로컬 보존·커밋 제외**(ADR-04와 같은 원칙).
